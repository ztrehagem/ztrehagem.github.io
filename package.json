{
  "name": "ztrehagem.github.io",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt generate",
    "serve": "nuxt start",
    "prod": "PUBLIC_PATH=https://ztrehagem.dev/ nuxt generate",
    "predeploy": "run-s clean prod",
    "deploy": "git subtree push --prefix dist origin master",
    "clean": "rm -r dist || true"
  },
  "devDependencies": {
    "json-loader": "^0.5.7",
    "npm-run-all": "^4.1.5",
    "nuxt": "^2.4.5",
    "pug": "^2.0.3",
    "pug-plain-loader": "^1.0.0",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2",
    "yaml-loader": "^0.5.0"
  }
}
